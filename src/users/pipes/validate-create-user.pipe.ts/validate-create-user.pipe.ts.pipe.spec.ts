import { ValidateCreateUserPipeTsPipe } from './validate-create-user.pipe.ts.pipe';

describe('ValidateCreateUserPipeTsPipe', () => {
  it('should be defined', () => {
    expect(new ValidateCreateUserPipeTsPipe()).toBeDefined();
  });
});
